# Dashboard

Single-page Streamlit dashboard reading live from `../dev.duckdb` (read-only — safe to run alongside `dbt build`).

```
pip install -r requirements.txt
streamlit run app.py
```

Opens automatically at http://localhost:8501.
